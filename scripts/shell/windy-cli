#!/usr/bin/env bash
WINDY_DIR="$HOME/Projects/windy-cli"

export WINDY_CALLER_DIR="$PWD"
cd "$WINDY_DIR" || { echo "windy-cli: $WINDY_DIR not found" >&2; exit 1; }
unset BUNDLE_GEMFILE BUNDLE_PATH BUNDLE_BIN BUNDLE_APP_CONFIG
exec bundle exec ruby exe/windy-cli "$@"
